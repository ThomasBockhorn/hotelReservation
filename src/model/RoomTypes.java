package model;

/**
 * RoomTypes is an enumeration that will only allow SINGLE and DOUBLE values for rooms.
 */

public class RoomTypes {
    public enum RoomType {
        SINGLE,
        DOUBLE
    }
}
